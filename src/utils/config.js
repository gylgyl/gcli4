export const USER_STORAGE = 'USER_STORAGE_G'
export const TOKEN_STORAGE = 'TOKEN_STORAGE_G'

// export 的数据是共享的 就是浅克隆 所以最好不要修改import 的数据

export const GPAge = {
  pageSize: 20,
  hasNextPage: false,
  pageNo: 1
}
